task_id: flow_obstacle_2d
# NOTE: bundle_root must be the directory that contains:
#   bundle/manifest.json
#   bundle/conditions.json
#   derived/points_*.parquet
bundle_root: data/artifacts/examples/quickstart_native
require_sensors: false
